<h1> Editing Article</h1>
<%= form_for :article, url: article_path(@article), method:
  :patch do |f| %>
  <%if @article.errors.any? %>
  <div id="error_explanation">
    <h2>
      <%= pluralize(@article.erors.count, "error") %>
      prohibited
      this article from being saved:
    </h2>
    
